{
  "name": "shree-plantation-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
